<?xml version="1.0" encoding="UTF-8"?>
<project default="xar" name="betmas-id-manager">
    <xmlproperty file="expath-pkg.xml"/>
    <property name="project.version" value="${package(version)}"/>
    <property name="project.app" value="betmas-id-manager"/>
    <property name="build.dir" value="build"/>
    <target name="xar">
        <mkdir dir="${build.dir}"/>
        <!-- node_modules needs the /** suffix to recurse - a bare "node_modules"
             pattern only excludes the directory entry itself, not its contents,
             and silently ships every dev dependency into the .xar. -->
        <zip basedir="." destfile="${build.dir}/${project.app}-${project.version}.xar" excludes="${build.dir}/** .github/** tests/** docker/** node_modules/** .husky/** .gitignore"/>
    </target>
</project>
