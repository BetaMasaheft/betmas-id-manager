name: exist-db CI

# Scope push to main only - a PR branch push would otherwise also fire
# push, double-triggering this workflow and showing a spurious red check.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: betmas-id-manager-ci-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install bats
        run: |
          sudo apt-get update
          sudo apt-get -y install bats

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - uses: docker/setup-buildx-action@v4

      # Bare public base (duncdrum/existdb + eeditiones/builder + the public
      # roaster xar) - unlike betmasweb/BetMasApi, nothing here is a private
      # ghcr.io package, so no registry login is needed just to build.
      - name: Build eXist image
        uses: docker/build-push-action@v7
        with:
          context: .
          tags: betmas-id-manager-exist:local
          load: true
          cache-from: type=gha,scope=exist
          cache-to: type=gha,mode=max,scope=exist

      - name: Start the stack
        run: docker compose up -d

      - name: Wait for eXist to start
        timeout-minutes: 2
        run: |
          until docker logs exist 2>&1 | grep -q "Server has started"; do
            echo "Still waiting..."
            sleep 2
          done
          # Bounded settle sleep, not a wait-for-healthy loop.
          sleep 15

      # Run bats before Cypress: several Cypress specs deliberately exercise
      # 400/404/409 error paths, and roaster logs those at ERROR level by
      # design, which would otherwise fail bats's "logs are error free" check.
      - name: Run bats smoke tests
        run: bats --tap tests/*.bats

      - name: Run Cypress tests
        uses: cypress-io/github-action@v7
        with:
          install: false

      - name: Show eXist logs on failure
        if: failure()
        run: docker logs exist

      - name: Upload Cypress screenshots
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: cypress-screenshots
          path: tests/cypress/screenshots/
